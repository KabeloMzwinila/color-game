# Color Game

This repo is a single Vite + React + TypeScript app.

Run locally with:

```bash
npm install
npm run dev
```

Build for production with:

```bash
npm run build
```

Source of truth:

- App component: `src/App.tsx`
- Vite entry: `src/main.tsx`
- HTML entry: `index.html`
